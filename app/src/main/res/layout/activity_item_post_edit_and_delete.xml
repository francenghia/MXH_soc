<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="#f2f2f2"
    tools:context=".ViewActivity.ItemPostEditAndDelete">

    <include
        android:id="@+id/toolbarEdit"
        layout="@layout/app_bar_layout" />
    <EditText
        android:id="@+id/edtContentPostEdit"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/toolbarEdit"
        android:layout_margin="20dp"
        android:background="@drawable/edit_round"
        android:hint="Viết gì đó đi nào !"
        android:padding="10dp" />

    <ImageView
        android:id="@+id/imagePostEdit"
        android:layout_width="match_parent"
        android:layout_height="250dp"
        android:layout_below="@id/edtContentPostEdit"
        android:layout_margin="20dp"
        android:src="@drawable/plus"
        android:scaleType="centerCrop"/>


      <Button
          android:id="@+id/btnCardSliderPhoto"
          android:elevation="10dp"
          android:layout_below="@+id/imagePostEdit"
          android:layout_width="200dp"
          android:text="@string/more_photo"
          android:textAllCaps="false"
          android:textSize="20sp"
          android:textColor="@android:color/white"
          android:background="@drawable/button_myphoto"
          android:layout_height="wrap_content"
          android:layout_centerInParent="true"
          android:layout_marginTop="50dp"/>


</RelativeLayout>